<%- include('../partials/header') %>

<% todos.forEach(function(t) { %>

        <title><%= %></title>
        
        <h1><%= title %></h1>
        <p>Welcome to <%= title %></p>

    </html>

<%- include('../partials/footer') %>